# Verification — 13 September 2026

The Windows Release build passed all 40 registered CTest checks (376.04 seconds). Tests run headlessly, including the hidden native Profile dialog. Coverage includes private voice 9, finish cues across modes/music engines, mute isolation, audio snapshots, all 20 Music Player selections, gameplay shuffle persistence, player setup banks, timing storage, static audio authority and source manifests.

After that suite, the final Rally return-path correction was checked in both generated dispatch implementations: final results enter the menu, and the next Start reaches native pre-race without consuming the stack. The final rebuild and focused regression pass cover that correction and integrity. The journey harness's additional Data self-test writes eight synthetic valid runs: five enter the table, and the next three remain durable last-run records outside the Top 5. Synthetic storage checks are not claimed as driven races.

Natural controller-driven acceptance:

- Las Vegas, USA track 1: two completed three-lap races with Retry between them. Final times 1:17.400 and 1:14.417. Both were persisted and correctly ordered on Overall Top 5. Finish returned to Time Trial with Country highlighted.
- Los Angeles, USA track 2: three completed three-lap races with two consecutive Retries. All handoffs retained course 1, Sidewinder, automatic gearbox and name A. Times were 2:10.817, 2:08.667 and 2:10.967. Screenshot order was 2:08.667, 2:10.817, 2:10.967. Finish returned to Time Trial with Los Angeles retained. The last run's independent best sectors were 10.950, 14.167 and 13.417 seconds. Physical Data save/reboot/reload passed.

Results, Retry, Top 5 and returned-menu screenshots wait for stable frames; the final menu waits 90 frames. Native races above preceded the final solo-position HUD adjustment and unrelated R-button/Rally handoff corrections; the final feature/regression tests cover the updated build. Production Retry has no run counter or coded limit; three consecutive natural runs do not empirically prove infinite execution.

Rally's held-Start Selected Tracks deadline passed at 300 emulated frames. Career screenshots show the five named leaders, last-lap time and readable checkpoint gaps. Colours were omitted because the per-racer mapping is unverified. All 20 music selections produced audio; menu/music changes preserve the separate ninth cue channel.

All automated timing/settings fixtures are isolated from user Data. The portable release ZIP contains no ROM or personal history. The local test copy includes the user's existing ROM, settings and Data files for immediate testing; packaging verifies their hashes against the original files. Source and executable hashes are verified when copied.

The adjacent Verification folder in the handoff contains the test logs and selected screenshots. This does not claim a new natural driving pass of every one of the 32 tracks or every car/gearbox combination.
